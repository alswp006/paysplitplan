// @ai-factory:placeholder
// 배선 선행(wiring-first)이 깐 자리 페이지다 — App.tsx에 `/result`로 이미 연결돼 있다.
// 이 화면을 담당하는 패킷은 이 파일을 **통째로 교체**하라(위 마커 주석 포함 — 마커가 남으면 산출물로 인정되지 않는다).
import { PageShell } from "../components/PageShell";
export default function Result() {
  return (
    <PageShell>
      <main data-testid="placeholder-result">
        <h1>배분 결과와 소득 구간 비교</h1>
        <p>이 화면은 준비 중이에요.</p>
      </main>
    </PageShell>
  );
}
